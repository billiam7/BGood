


<%= form_tag searches_index_path do %>
 <%= search_field_tag :term %>
 <%= submit_tag "Go!" %>
<% end %>
<% @data_selection.each do |opportunity| %>
  <% @opportunities_keys.zip(@opportunity).each |key, opportunity| %>
    <ul>
      <li><%= "#{key}: #{opportunity}" %></li>
    </ul>
  <% end %>
<% end %>
